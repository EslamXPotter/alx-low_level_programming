#include "main.h"
#include <stdio.h>

/**
 * error-file - function that checks if files can be opened
 * @file_from: file from
 * @file_to: file to
 * @argv: array of pointers to the arguments
 * Return: o on success
 */

void error_file(int file_from, int file_to, char *argv[])

